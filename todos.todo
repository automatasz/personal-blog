✔ Create a skeleton for batch page @done(25-08-31 14:37)
☐ Add settings for:
  ☐ More or less keywords
☐ Make the delete button in imageuploader red
✔ Create my account page @started(25-09-08 18:40) @done(25-09-19 00:01) @lasted(1w3d5h21m58s)
☐ Create a payment page
☐ Create a way for users to be identified if they paid or not
☐ Create a way for users to upload full sized images
  ☐ get descriptions
  ☐ write descriptions into metadata
  ☐ download image with metadata embedded
✔ FIX: navbar links and search field disappear when user enters batch page. @done(25-08-31 22:47)
✔ display file name of the image in the batches page @done(25-08-31 21:36)
✔ insert info about failed batches in the database. let's say the description fails for one image -> set image status as fail, otherwise success @done(25-08-31 21:38)
☐ add export for the whole batch of images in batch page
☐ add export button for each image in batch page
✔ add tooltip for logout/login button so it isn't confusing. @done(25-09-01 00:13)
✔ instead of the door + arrow icon, use avatar -> when it is pressed a menu opens with option to login/logout and/or go to profile. @done(25-09-01 00:13)
✔ in the menu have the full text descriptions such as "profile" or "sign in" or "sign out" instead of icons. @done(25-09-01 00:14)
✔ in the menu mention what user is logged in (i.e. email of the user). @done(25-09-08 18:02)
☐ add meaningful errors for users:
  ☐ if inngest fails creating the events
  ☐ if batch isn't created
  ☐ if uploadthing doesn't upload the images
  ☐ if images cannot be found in the uploadthing after upload
☐ integrate stripe:
  ☐ payment page for user.
  ☐ add tokens to user.
  ☐ calculate the cost for a batch.
  ☐ remove tokens when a batch is created.
☐ bug: the session is not checked and the action is executed with an expired session while the ui displays that the user is logged in
☐ save the size of the image so that when the batch is displayed there is no layout shift
☐ create titles for batches with llm
☐ make the pills of the description keywords clickable -> when clicked copy to clipboard
☐ make a trial for non-admin user -> the user can upload up to 10 images and then view the results
☐ a view of all uploaded images -> click on image -> go to batch
☐ FIX: bug no auth panel in mobile mode
☐ store image width and height to make sure the images do not cause layout shift